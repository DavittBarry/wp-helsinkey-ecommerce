<?xml version="1.0"?>
<ruleset name="Project">
    <description>The coding standard for my project.</description>

    <!-- Exclude specific files and folders -->
    <exclude-pattern>/var/www/html/wp-config.php</exclude-pattern>

    <exclude-pattern>/var/www/html/wp-config-docker.php</exclude-pattern>

    <rule ref="PEAR">
        <exclude name="PEAR.Commenting.FileComment.Missing"/>
    </rule>
</ruleset>